server:
  port: 80
# DİKKAT
# Ana başlık ile alt başlığı ayırmak için bir TAB
# tuşu kullanmalıyız; çok dikkatli olun. Ve ':'
# (iki nokta üst üste)den sonra bir boşluk
# bırakılmalıdır.
# Ve ilk başladığınız satırda tab koymayın.

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/MonolitikApp
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
